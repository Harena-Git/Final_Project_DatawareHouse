name: 'salon_dw'
version: '1.0.0'
config-version: 2

profile: 'salon_dw'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  salon_dw:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: dwh
